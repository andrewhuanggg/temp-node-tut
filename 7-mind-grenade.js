const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is ${num1+num2}`)
}

addValues(); //if there is a function inside of the module that we invoke, then the function will run 